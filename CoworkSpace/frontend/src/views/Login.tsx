import LoginForm from "../components/login/LoginForm";

const Login = () => {
  return (
    <section className="layout">
      <h2>Connexion</h2>
      <LoginForm />
    </section>
  );
};

export default Login;
